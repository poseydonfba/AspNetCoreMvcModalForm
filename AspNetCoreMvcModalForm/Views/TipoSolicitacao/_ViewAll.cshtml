@inject Microsoft.AspNetCore.Antiforgery.IAntiforgery antiforgeryProvider
@model IEnumerable<AspNetCoreMvcModalForm.Business.Models.TipoSolicitacao>

@{
    Layout = null;
}

<vc:summary></vc:summary>

<div class="row">
    <div class="col-md-6"><h1>Tipo de Solicitações</h1></div>
    <div class="col-md-6 text-end"><a onclick="jQueryModalGet(this, '@Url.Action("Create", "TipoSolicitacao")', 'Novo Tipo de solicitação', 'modalTipoSolicitacao')" class="btn btn-outline-dark btn-sm"><i class="fas fa-random"></i> Novo tipo</a></div>
</div>

<table class="table table-striped table-sm table-hover" id="tableTipoSolicitacao" style="width:100%;">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.Descricao)</th>
            <th></th>
        </tr>
    </thead>
</table>